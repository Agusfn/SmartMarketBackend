import { AccountManagerI } from "./AccountManagerFactory"

export default class AlpacaAccountManager implements AccountManagerI {

    async buy(symbol: string, amount: number): Promise<any> {

    }
    
    async sell(symbol: string, amount: number): Promise<any> {

    }
}